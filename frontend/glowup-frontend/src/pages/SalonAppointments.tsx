import { Layout } from '../components/layout/Layout';

export const SalonAppointments = () => {
    return (
        <Layout>
            <h1>Salon Appointments</h1>
            <p>View and manage appointments for your salons.</p>
        </Layout>
    );
}; 