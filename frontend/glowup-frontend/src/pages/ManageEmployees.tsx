import { Layout } from '../components/layout/Layout';

export const ManageEmployees = () => {
    return (
        <Layout>
            <h1>Manage Employees</h1>
            <p>Add and manage employees for your salons.</p>
        </Layout>
    );
}; 